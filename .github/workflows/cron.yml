name: schedule workflow

  # schedule:
  #   - cron: '0 0 * * *'

on:
    push:
        branches:
            - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        - run: echo "atualiza às 00:00h"